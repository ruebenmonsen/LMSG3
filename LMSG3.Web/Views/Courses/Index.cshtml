@model IndexCourseViewModel

<p>
    <a asp-action="CreateCourse">Create New</a>
</p>
<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th class="col-1">
               
            </th>
            <th class="col-2">
                @Html.DisplayNameFor(model => model.CoursesList.FirstOrDefault().Name)
            </th>
            <th class="col-2">
                @Html.DisplayNameFor(model => model.CoursesList.FirstOrDefault().StartDate)
            </th>
            <th class="col-5">
                @Html.DisplayNameFor(model => model.CoursesList.FirstOrDefault().Description)
            </th >
            <th class="col-2"></th>
        </tr>
    </thead>
    <tbody>
        <partial name="CourseListPartial" model="@Model.CoursesList" />
    </tbody>
</table>
